#configurazione del server web2

ifconfig eth0 1.0.0.1/24 up
route add default gw 1.0.0.9 dev eth0

/etc/init.d/apache2 start


#commenti:
#Imposto l'indirizzo IP della scheda di rete eth0 su 1.0.0.1 con una subnet mask indicata con la notazione /24 (255.255.255.0).
#Aggiungo una "via" di default alla tabella di routing del client, 
#segnalando che i pacchetti destinati ad un IP esterno alla pubblic network 1 devono essere instradati attraverso 
#l'interfaccia di rete eth0 e inviati al gateway con IP: 1.0.0.9

#Comando utilizzato per avviare il server web Apache sulla maggior parte delle distribuzioni Linux.